@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model List<UserRoleModel>
@{
    var rID = ViewData["rID"];
}
@{
    <form method="post" class="form-control" asp-action="EditRoleUser">
        <table class="table table-responsive">
            <h1 class="modal-title">Update Role Users</h1>
            <tr>
                <th> User </th>
                <th>In Role</th>
            </tr>


            @for (int i = 0; i < @Model.Count(); i++)
            {
                <tr>
                    <td>
                        @Model[i].Name
                    </td>
                    <td>
                        <input asp-for="@Model[i].Name" type="hidden" />
                        <input asp-for="@Model[i].Id" type="hidden" />
                        <input asp-for="@Model[i].rId" type="hidden" />
                        <input asp-for="@Model[i].rName" type="hidden" />
                        <input class="form-check-input" asp-for="@Model[i].IsSelected" type="checkbox" />
                    </td>
                </tr>
            }
        </table>
        <button type="button"  class="btn btn-secondary btn-sm"><a class="text-decoration-none" style="color: white;" asp-action="EditRole" asp-route-id="@rID">Cancel</a></button>
        <button class="btn btn-primary btn-sm" type="submit">Save changes</button>
    </form>
}
